<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhangli.myapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_img_music"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MusicService" />

        <activity
            android:name=".MusicCursorListActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MusicActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SaomiaoActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".MusicListActivity" />


        <activity android:name=".TwoMusicListActivity" />

    </application>

</manifest>
